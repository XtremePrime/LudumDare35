#ifndef ANIMATION_HPP
#define ANIMATION_HPP

#include <SFML/Graphics.hpp>

class Animation
{
/*private:
	sf::Texture texture;
	std::vector<sf::IntRect> frames;
public:
	void add_frame(sf::IntRect rect)
	{
		frames.push_back(rect);
	}

	void set_spritesheet(sf::Texture& texture){ this->texture = texture; }
	sf::Texture& get_spritesheet(){ return this->texture; }
	const sf::IntRect& get_frame(std::size_t idx){ return frames[idx]; }
	std::size_t get_size(){ return frames.size(); }*/
};

#endif // ANIMATION_H